# 2026-03-22 - Session Notes

## MiroFish GPU Fix ✅
**Problem:** Consensus connector was getting 500 errors from MiroFish ontology generation
**Root cause:** Cold start timeout - qwen2.5:14b takes ~7s to load 9GB into VRAM, then ~60s for ontology gen. First requests were timing out at ~14s.

**Fix applied:**
1. Added `warm_up_llm()` function to `consensus_swarm_connector.py` - pings Ollama before running sims
2. Increased `request_timeout` to 300s in MiroFishClient initialization
3. Tested successfully - ontology generation now completes in ~66s

## Connector Scheduled ✅
Created Windows Task Scheduler tasks:
- `MiroFishConnector_Noon` - runs at 12:00 PM daily
- `MiroFishConnector_3PM` - runs at 3:00 PM daily
- Both execute `C:\Users\USER\clawd\mirofish-hub\run_connector.bat`

## Whale API Dashboard Routes ✅
Updated `whale_api.py` to serve HTML pages directly:
| Route | File |
|-------|------|
| `/` | whale-tracker-original.html |
| `/consensus` | whale-consensus.html |
| `/leaderboard` | whale-tracker-v2.html |
| `/dashboard` | dashboard.html |

**Access:** http://100.115.12.91:8081/consensus shows all whale consensus picks

## Key Files Modified
- `mirofish-hub/consensus_swarm_connector.py` - added warm_up_llm(), increased timeout
- `mirofish-hub/whale_api.py` - added static file routes
- `mirofish-hub/run_connector.bat` - created launcher script

## System Status
- RTX GPU: Working, model loads in ~7s
- MiroFish: Operational on :5001
- Whale API: Operational on :8081
- Mac Mini: All services UP

---

## Consensus Pipeline v2 Fix (Rusty's work - 19:18)

**Major fix by Rusty** - traced stale data through entire pipeline:

### Root Cause
- Markets like Lakers vs Rockets (ended 3/19) sitting as `pending`
- Outcome tracker hadn't resolved them
- `/api/consensus` was showing dead markets

### Files Modified (by Rusty)
1. `whale_scorer.py` - cache endDate from Gamma API
2. `whale_hunter_connector.py` - store end_date on INSERT
3. `whale_api.py` - SQL filter + `_filter_closed_markets()` using clob_token_ids
4. `consensus_swarm_connector.py` - `refresh_positions()`, new CLI flags, 120s timeout
5. `whale_hunter.db` - end_date column + WAL mode

### Key Discovery
Gamma API `condition_id` query returns random/wrong results. Must use `clob_token_ids` for reliable market status.

### Result
- Before: 8 stale signals from dead markets
- After: 4 LIVE picks (Real Madrid, Atletico, St. John's, Trail Blazers)

---

## SOUL.md Updated (20:09)
Added "Consensus Pipeline v2" section documenting Rusty's stale data fix with full architecture diagram and commands.
